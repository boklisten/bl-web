import { async, ComponentFixture, TestBed } from "@angular/core/testing";

import { InfoAgreementPrivacyComponent } from "./info-agreement-privacy.component";
/*
describe('InfoAgreementPrivacyComponent', () => {
	let component: InfoAgreementPrivacyComponent;
	let fixture: ComponentFixture<InfoAgreementPrivacyComponent>;

	beforeEach(async(() => {
		TestBed.configureTestingModule({
			declarations: [InfoAgreementPrivacyComponent]
		})
			.compileComponents();
	}));

	beforeEach(() => {
		fixture = TestBed.createComponent(InfoAgreementPrivacyComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		// expect(component).toBeTruthy();
	});
});
*/
