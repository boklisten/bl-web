import { BlcGoBackDirective } from './blc-go-back.directive';

describe('BlcGoBackDirective', () => {
  it('should create an instance', () => {
    const directive = new BlcGoBackDirective();
    expect(directive).toBeTruthy();
  });
});
