import { BlcClickOutsideDirective } from './blc-click-outside.directive';

describe('BlcClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new BlcClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
