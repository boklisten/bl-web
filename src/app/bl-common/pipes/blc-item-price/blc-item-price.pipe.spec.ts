import { BlcItemPricePipe } from './blc-item-price.pipe';

describe('BlcItemPricePipe', () => {
  it('create an instance', () => {
    const pipe = new BlcItemPricePipe();
    expect(pipe).toBeTruthy();
  });
});
